{
  "name": "your-module",
  "version": "1.0.0",
  "description": "A tiny module with Node, TypeScript, and top-level await support",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx/esm --test \"./src/**/*.test.ts\""
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.2",
    "typescript": "^4.9.0"
  },
  "dependencies": {
    "@types/assert": "^1.5.11"
  }
}
